[package]
name = "soromint-lifecycle"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lifecycle.rs"

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
